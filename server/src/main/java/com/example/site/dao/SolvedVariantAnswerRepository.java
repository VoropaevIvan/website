package com.example.site.dao;

import com.example.site.dto.SolvedVariantAnswer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SolvedVariantAnswerRepository extends JpaRepository<SolvedVariantAnswer, Long> {
}
