package com.example.site.dao;

import com.example.site.dto.SolvedVariantVerdict;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SolvedVariantVerdictRepository extends JpaRepository<SolvedVariantVerdict, Long> {
}
